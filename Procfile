web: sh -c 'set -e; if ! command -v dot >/dev/null 2>&1; then echo "Installing graphviz..." >&2; apt-get update && apt-get install -y graphviz; fi; command -v dot && dot -V; PYTHONPATH=/app exec python -m uvicorn web.app.asgi:app --host 0.0.0.0 --port ${PORT:-8080}'
