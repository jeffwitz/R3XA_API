[build]
cmd = "python -m pip install -r requirements.txt && python -m pip install '.[web]'"

[deploy]
startCommand = "PYTHONPATH=/app python -m uvicorn web.app.asgi:app --host 0.0.0.0 --port ${PORT:-8000}"
